# Absolute origin, used for canonical URLs, hreflang and JSON-LD.
# Without it the build falls back to http://localhost:3000 so the wrong value is
# obvious in the markup rather than silently shipping someone else's domain.
NEXT_PUBLIC_SITE_URL=https://your-domain.example
